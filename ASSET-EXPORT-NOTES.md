# Asset note

The `assets/` folder contains the supplied and generated AMI Liberia visual assets. The website source currently uses lifecycle-managed `/manus-storage/...` URLs so the active Manus website can load them without bundling large files into the deployed build.
